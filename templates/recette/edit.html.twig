{% extends 'base.html.twig' %}

{% block title %}Edit Recette{% endblock %}

{% block body %}
    <h1>Edit Recette</h1>

    {{ include('recette/_form.html.twig', {'button_label': 'Mise à jour'}) }}

    <a class="btn btn-primary" href="{{ path('app_recette_index') }}" role="button">Retour vers l'accueil</a>
{% if is_granted('ROLE_ADMIN') %}
    {{ include('recette/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
